<index.html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="New Year Bash 2025 - Invite Only">
    <title>The House Party | NYE 2025</title>

    <meta property="og:title" content="NEW YEAR BASH 2025">
    <meta property="og:description" content="Good music, bad decisions, and aesthetic memories. Jan 4th.">
    <meta property="og:image" content="https://picsum.photos/1200/630"> <meta property="og:url" content="https://yourwebsite.com">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Abril+Fatface&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        /* --- CSS VARIABLES --- */
        :root {
            --bg-color: #0a0a0a;
            --text-color: #ffffff;
            --accent-color: #ff3b3b;
            --glass-bg: rgba(255, 255, 255, 0.05);
            --transition-speed: 0.3s;
        }

        /* --- RESET & BASE --- */
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Inter', sans-serif;
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* --- UTILITIES --- */
        /* Screen Reader Only: Hides text visually but keeps it available for A11y */
        .sr-only {
            position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
            overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border-width: 0;
        }

        /* --- TYPOGRAPHY --- */
        h1, h2, .logo { font-family: 'Abril Fatface', cursive; letter-spacing: 2px; }

        /* --- NAVIGATION --- */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 5vw; /* Responsive padding */
            position: fixed;
            width: 100%;
            z-index: 100;
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px); /* Safari support */
            background: rgba(10, 10, 10, 0.6);
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }
        .logo { font-size: 1.5rem; font-weight: bold; }
        .menu a {
            color: var(--text-color);
            text-decoration: none;
            margin-left: 30px;
            font-size: 0.9rem;
            text-transform: uppercase;
            transition: color var(--transition-speed);
        }
        .menu a:hover { color: var(--accent-color); }
        .cta-btn { 
            border: 1px solid var(--text-color); 
            padding: 8px 24px; 
            border-radius: 50px; 
        }
        .cta-btn:hover { 
            background: var(--text-color); 
            color: var(--bg-color) !important; 
        }

        /* --- HERO SECTION --- */
        .hero {
            /* Use dvh (Dynamic Viewport Height) for mobile browser address bar handling */
            height: 100dvh; 
            min-height: 600px;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
            background: radial-gradient(circle at center, #1a1a1a 0%, #000000 100%);
            /* Prevent content shift during parallax */
            will-change: transform; 
        }
        .hero-content {
            z-index: 2;
            padding: 0 20px;
        }
        .hero h1 {
            font-size: clamp(3rem, 8vw, 6rem); /* Responsive font scaling */
            line-height: 1.1;
            margin: 20px 0;
            text-shadow: 0 0 30px rgba(255, 255, 255, 0.15);
        }
        .eyebrow { color: var(--accent-color); letter-spacing: 4px; font-weight: 600; font-size: 0.9rem; }
        .subtitle { opacity: 0.8; margin-bottom: 40px; font-size: 1.1rem; }
        
        .scroll-down-btn {
            background: none;
            border: 1px solid rgba(255,255,255,0.2);
            color: white;
            width: 50px; height: 50px;
            border-radius: 50%;
            cursor: pointer;
            animation: bounce 2s infinite;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            transition: border-color 0.3s;
        }
        .scroll-down-btn:hover { border-color: var(--accent-color); color: var(--accent-color); }

        /* --- MASONRY GRID --- */
        .gallery-section { padding: 100px 5%; background: var(--bg-color); position: relative; z-index: 10; }
        .gallery-section h2 { text-align: center; font-size: 3rem; margin-bottom: 50px; }
        
        .masonry-grid { 
            column-count: 4; 
            column-gap: 20px; 
        }
        .grid-item { 
            margin-bottom: 20px; 
            break-inside: avoid; /* Prevents images from being cut in half between columns */
        }
        .grid-item img {
            width: 100%;
            border-radius: 12px;
            display: block;
            transition: transform 0.3s ease, filter 0.3s ease;
            filter: grayscale(40%);
        }
        .grid-item img:hover { 
            transform: scale(1.02); 
            filter: grayscale(0%); 
            box-shadow: 0 10px 30px rgba(255, 59, 59, 0.2); 
        }

        /* --- DETAILS SECTION --- */
        .details-section {
            display: flex; justify-content: center; padding: 80px 5%;
            background: #0f0f0f; border-top: 1px solid #222; border-bottom: 1px solid #222;
            flex-wrap: wrap; gap: 60px;
        }
        .info-card { text-align: center; min-width: 200px; }
        .info-card i { font-size: 2rem; color: var(--accent-color); margin-bottom: 15px; }
        .info-card h3 { margin-bottom: 10px; letter-spacing: 1px; }
        .info-card p { opacity: 0.7; font-size: 0.95rem; }

        /* --- RSVP SECTION --- */
        .rsvp-section { padding: 100px 20px; text-align: center; }
        .form-container { max-width: 400px; margin: 0 auto; }
        
        input {
            width: 100%; padding: 15px; margin-bottom: 15px;
            background: var(--glass-bg); border: 1px solid #333;
            color: white; border-radius: 8px; outline: none;
            transition: border-color 0.3s;
            font-family: inherit;
        }
        input:focus { border-color: var(--accent-color); background: rgba(255,255,255,0.1); }
        
        button[type="submit"] {
            width: 100%; padding: 15px; background: var(--accent-color);
            color: white; border: none; font-weight: bold;
            border-radius: 8px; cursor: pointer; font-size: 1rem; letter-spacing: 1px;
            transition: transform 0.2s, background 0.3s;
        }
        button[type="submit"]:hover { transform: translateY(-2px); filter: brightness(1.1); }
        button[type="submit"]:active { transform: translateY(0); }

        footer { text-align: center; padding: 50px; opacity: 0.5; font-size: 0.8rem; letter-spacing: 1px; }

        /* --- MEDIA QUERIES --- */
        @media (max-width: 1024px) { .masonry-grid { column-count: 3; } }
        @media (max-width: 768px) {
            .masonry-grid { column-count: 2; }
            .navbar { padding: 20px; }
            .menu { display: none; } /* Consider adding a hamburger menu for full implementation */
            .details-section { gap: 40px; }
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {transform: translateY(0);}
            40% {transform: translateY(-10px);}
            60% {transform: translateY(-5px);}
        }
    </style>
</head>
<body>

    <nav class="navbar" role="navigation">
        <div class="logo">NEW YEAR BASH</div>
        <div class="menu">
            <a href="#vibe">The Vibe</a>
            <a href="#details">Details</a>
            <a href="#rsvp" class="cta-btn">RSVP</a>
        </div>
    </nav>

    <header class="hero" id="home">
        <div class="hero-content">
            <p class="eyebrow">JAN 4 • 9:00 PM</p>
            <h1>NEW YEAR BASH<br>SESSION</h1>
            <p class="subtitle">Good music, bad decisions, and aesthetic memories.</p>
            <button id="scrollBtn" class="scroll-down-btn" aria-label="Scroll to content">
                <i class="fa-solid fa-arrow-down"></i>
            </button>
        </div>
    </header>

    <main>
        <section id="vibe" class="gallery-section">
            <h2>THE MOODBOARD</h2>
            <div class="masonry-grid">
                <div class="grid-item"><img src="https://picsum.photos/600/800?random=1" alt="Party Vibe 1" loading="lazy"></div>
                <div class="grid-item"><img src="https://picsum.photos/600/600?random=2" alt="Party Vibe 2" loading="lazy"></div>
                <div class="grid-item"><img src="https://picsum.photos/600/900?random=3" alt="Party Vibe 3" loading="lazy"></div>
                <div class="grid-item"><img src="https://picsum.photos/600/700?random=4" alt="Party Vibe 4" loading="lazy"></div>
                <div class="grid-item"><img src="https://picsum.photos/600/500?random=5" alt="Party Vibe 5" loading="lazy"></div>
                <div class="grid-item"><img src="https://picsum.photos/600/850?random=6" alt="Party Vibe 6" loading="lazy"></div>
                <div class="grid-item"><img src="https://picsum.photos/600/650?random=7" alt="Party Vibe 7" loading="lazy"></div>
                <div class="grid-item"><img src="https://picsum.photos/600/750?random=8" alt="Party Vibe 8" loading="lazy"></div>
            </div>
        </section>

        <section id="details" class="details-section">
            <div class="info-card">
                <i class="fa-solid fa-location-dot"></i>
                <h3>WHERE</h3>
                <address style="font-style: normal;">
                    CHATTARPUR<br>New Delhi
                </address>
            </div>
            <div class="info-card">
                <i class="fa-solid fa-shirt"></i>
                <h3>DRESS CODE</h3>
                <p>Streetwear / Retro<br>Keep it Aesthetic</p>
            </div>
            <div class="info-card">
                <i class="fa-solid fa-bottle-water"></i>
                <h3>BYOB</h3>
                <p>We provide mixers.<br>You bring the poison.</p>
            </div>
        </section>

        <section id="rsvp" class="rsvp-section">
            <div class="form-container">
                <h2>ARE YOU IN?</h2>
                <form id="rsvpForm">
                    <label for="name" class="sr-only">Your Name</label>
                    <input id="name" type="text" placeholder="Your Name" required autocomplete="name">
                    
                    <label for="insta" class="sr-only">Instagram Handle</label>
                    <input id="insta" type="text" placeholder="Instagram Handle" required autocomplete="off">
                    
                    <button type="submit">CONFIRM ENTRY</button>
                </form>
            </div>
        </section>
    </main>

    <footer>
        <p>INVITE ONLY • 2025</p>
    </footer>

    <script>
        /* --- OPTIMIZED JAVASCRIPT --- */

        // 1. Smooth Scroll Handling
        document.getElementById('scrollBtn').addEventListener('click', function() {
            document.getElementById('vibe').scrollIntoView({ behavior: 'smooth' });
        });

        // 2. RSVP Form Logic
        document.getElementById('rsvpForm').addEventListener('submit', function(e) {
            e.preventDefault(); 
            const btn = this.querySelector('button');
            const originalText = btn.innerText;

            btn.innerText = "YOU'RE ON THE LIST";
            btn.style.backgroundColor = "#4BB543"; 
            btn.style.color = "#fff";
            btn.disabled = true; // Prevent double submission

            // In a real app, you would send data to a backend (Firebase/SheetDB) here
            setTimeout(() => { 
                alert("Screenshot this screen as your entry ticket!"); 
            }, 500);
        });

        // 3. Performance Optimized Parallax (Debounced via requestAnimationFrame)
        // This runs at 60fps instead of choking the main thread
        let ticking = false;
        const heroContent = document.querySelector('.hero-content');

        window.addEventListener('scroll', function() {
            if (!ticking) {
                window.requestAnimationFrame(function() {
                    const scrolled = window.scrollY;
                    
                    // Stop calculating if the hero is out of view to save resources
                    if (scrolled < window.innerHeight) {
                        // translate3d forces GPU acceleration
                        heroContent.style.transform = `translate3d(0, ${scrolled * 0.4}px, 0)`;
                        heroContent.style.opacity = 1 - (scrolled / 700);
                    }
                    
                    ticking = false;
                });
                ticking = true;
            }
        });
    </script>
</body>
</html>